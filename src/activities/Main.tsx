import { type ActivityComponentType } from '@stackflow/react';
// component
import { AppScreen } from '@components/index';

export const Main: ActivityComponentType = () => {
    return <AppScreen main>main page</AppScreen>;
};
